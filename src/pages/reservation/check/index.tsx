import Header from '@/components/Header';
import React from 'react';
import CheckTitle from './CheckTitle';
import CheckMiddleContent from './CheckMiddleContent';

function Check() {
  return (
    <main>
      <Header />
      <CheckTitle />
      <CheckMiddleContent />
    </main>
  );
};

export default Check;
